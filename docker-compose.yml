version: '3'
services:
  ###
  # services
  ###
  django:
    build: .
    network_mode: host
    command: python manage.py runserver 0.0.0.0:3001
    volumes:
        - .:/app
    ports:
        - "3001:3001"
    env_file: .env
    tty: true
    stdin_open: true